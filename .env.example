# Copy to .env and fill in a real value — used only by docker-compose.yml
# to set the SQL Server container's `sa` password. Must meet SQL Server's
# complexity requirements (8+ chars, upper+lower+digit+symbol).
MSSQL_SA_PASSWORD=Change_Me_123!
